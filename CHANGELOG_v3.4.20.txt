PrimeTac AUTO v3.4.20 SAFE VARIANT MATCH

- Исправлено главное: XML group_id теперь считается точной семьёй разновидностей только при ТОЧНОМ сопоставлении товара Prom с offer поставщика (SKU/ID/точное название), а не по fuzzy-сходству >=70%.
- Индекс поставщика теперь учитывает и vendorCode/SKU, и offer id.
- Явный конфликт бренда блокирует даже совпавший числовой ID/SKU.
- Перед тестом вся семья проходит coherence-проверку: один и тот же XML group_id, сильная идентификация, единый бренд и похожая модель.
- Целевая группа PrimeTac выбирается голосованием классификации ВСЕХ членов семьи; случайный fallback по текущей группе удалён.
- Если тест SUCCESS, но разновидности не появились, а тест временно менял группы, исходные group_id товаров автоматически откатываются.
- При ошибке теста также выполняется best-effort откат исходных групп.
- Статус теста показывает исходный group_id каждого товара.
- Массовое восстановление всё ещё заблокировано: только одна тестовая семья.
