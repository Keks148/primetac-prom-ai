PrimeTac AUTO v3.4.18 SELF-HEAL VARIANT PROBE

- Тест одной семьи больше не зависит от нулевого RAM-state после Deploy.
- Перед preview заново получает товары Prom, с повторами загружает оба XML-фида, заново сопоставляет Prom↔XML и строит точные group_id-семьи.
- Не сохраняет деградированный аудит без XML group_id поверх предыдущего успешного отчёта.
- Разрешает тестовую семью до 20 позиций.
- Если готовое сопоставление PrimeTac-группы отсутствует, выбирает наиболее частую реальную текущую группу внутри точной XML-семьи.
- Массового объединения нет: только preview и один тест.
