PrimeTac AUTO v3.4.1 STOCK BUTTON FIX

Исправлено только ручное включение Stock Monitor:
- вместо ссылки теперь большая кнопка;
- при нажатии статус меняется сразу;
- ручной запуск выполняется напрямую, а не через отложенный setTimeout;
- если основной процесс уже завершён, зависший supplierState.loading сбрасывается;
- страница автообновляется, пока Stock Monitor работает.

ВАЖНО: STOCK_WRITE_ENABLED по-прежнему false по умолчанию.
То есть эта версия только проверяет остатки и НИЧЕГО не записывает в Prom, пока ты отдельно не включишь запись.
