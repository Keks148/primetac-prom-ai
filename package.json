{
  "name": "primetac-sync",
  "version": "1.0.0",
  "private": true,
  "description": "PrimeTac Sync v1.0 - read-only audit for Prom.ua, BEZET and Militaris",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/config.js && node --check src/prom.js && node --check src/suppliers.js && node --check src/audit.js && node --check src/ui.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "fast-xml-parser": "^5.2.5"
  }
}
