{
  "name": "primetac-editor",
  "version": "2.0.0",
  "private": true,
  "description": "PrimeTac post-import editor for Prom.ua",
  "main": "editor-server.js",
  "scripts": {
    "start": "node editor-server.js",
    "check": "node --check editor-server.js && node --check src/prom-editor.js && node --check src/config.js && node --check src/prom.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "fast-xml-parser": "^5.2.5"
  }
}
