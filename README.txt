PrimeTac Card Manager v1.9.1 SCAN FIX

Исправлено:
- если после Deploy состояние пустое, каталог автоматически начинает сканироваться при открытии панели;
- ручная кнопка "Сканировать весь каталог" остаётся;
- backend больше не проглатывает ошибку сканирования;
- ошибка Prom/API отображается прямо под кнопками;
- если PROM_TOKEN отсутствует, панель пишет об этом явно;
- polling не прекращается слишком рано сразу после запуска;
- поставщики BEZET + Militaris и Supplier Autofill из v1.9 сохранены.

После Deploy:
1. Открой страницу.
2. Через ~1 секунду должно появиться "Сканирование запущено".
3. Затем будет прогресс: обработано / всего.
4. Если Prom вернёт ошибку, её текст будет виден на странице.

Обязательные Environment:
PROM_TOKEN=...
WRITE_ENABLED=true

Остальные настройки можно не добавлять.
