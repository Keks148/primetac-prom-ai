PrimeTac Card Manager v1.9.4 SERVER PROGRESS

Исправлено главное: SERVER MODE теперь показывает реальный прогресс массовых операций даже если JavaScript браузера не работает.

Показывает:
- 1611 товаров и аудит прямо сервером;
- прогресс автозаполнения описаний/характеристик;
- обработано / подтверждено Prom / ошибки;
- первые ошибки;
- таблицу первых 120 товаров без JavaScript;
- явное пояснение, если category attributes недоступны через Products API.

Render: PROM_TOKEN и WRITE_ENABLED=true остаются как раньше.
