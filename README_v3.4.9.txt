PrimeTac AUTO v3.4.9

Deploy as the previous version. Keep:
AUTO_STATE_PATH=/var/data/primetac-auto-state.json

After deploy, do not create multiple imports. Use "Проверить результат Prom" first.
The UI now shows both all configured PrimeTac nodes and active nodes expected for the
current product distribution.

The next full run sends a structurally complete YML offer with description while the
Prom import settings still restrict writes to group + attributes only.
