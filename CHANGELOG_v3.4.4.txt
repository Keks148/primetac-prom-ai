PrimeTac AUTO v3.4.4 FINAL WAIT — изменения

1. Исправлен ложный финал после ACCEPTED_BACKGROUND.
   Теперь временная ошибка /products/import/status/{id} не переводит весь цикл в «Готово».

2. Ожидание импорта Prom:
   - опрос статуса: раз в 20 сек;
   - основное ожидание: до 15 мин;
   - проверка фактических групп каталога: раз в 60 сек;
   - после таймаута фоновая проверка продолжается автоматически.

3. Защита от дублей:
   - пока импорт не подтверждён, новый ручной и плановый импорт блокируется;
   - если Prom уже занят другим импортом, второй импорт не отправляется;
   - Stock Monitor ждёт завершения импорта.

4. Проверка результата:
   - сверяется не просто «товар находится в одной из наших групп», а конкретный group_id каждого товара с текущим планом;
   - сохраняется baseline до импорта, чтобы старое правильное распределение не выдавалось за доказательство нового импорта;
   - характеристики помечаются подтверждёнными только когда их реально можно прочитать и сверить через API.

5. Статистика SEO:
   - отдельно считаются «проверено», «требовало изменения», «изменено» для ключей и описаний.

6. После Render Deploy AUTO_ON_START по умолчанию false.
   Это сделано специально: деплой не должен создавать второй импорт поверх уже работающего.
   Плановый запуск каждые AUTO_INTERVAL_HOURS остаётся включённым.

Проверки перед упаковкой:
- node --check server.js: OK
- runtime smoke test со stub-зависимостями: OK
